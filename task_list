PICKED TASKS

 * print minified sql statement for any sql statement, not this custom function like form

 * write function to evaluate sql_expression

 * start with converting this parsed query into this exact logical query plan, using building blocks like
   * join left/right/inner/outer
   * aggregation with/without group_by_expressions, with/without aggregate_expressions
   * projection (add drop column attribute based projection if there is no need of it in later stages of the pipeline) (computed column can be computed at last most time)
   * selection (and can be broken into several stages alloing operator push down)
   * relation_scan (on range selection push down can become index scan)
   * union
   * sort (may be eliminated if the input row is already sorted from previous sorting or in index scan)
   * limit/offset operator

 * create SQL_TYPE
 * alter SQL_TYPE

TO BE PICKED TASKS

*0
 remaining parts of parsing DDL query
 * create queries
	SQL_FUNCTION, // only create and drop for function and procedure
	SQL_PROCEDURE,
	SQL_DOMAIN,
	SQL_SEQUENCE,
	SQL_TRIGGER,
 * alter queries
	SQL_DOMAIN,
	SQL_SEQUENCE,
	SQL_TRIGGER,

*1 add WITH keyword in CTEs

*2 prepared statements and the dynamic sql
 * named paramaters can be a problem, if we also do constant unfolding and we might loose the order of the params and may end up with wrong params at wrong places
 * we need to support just ?, ?1, ?2, etc and also :a, :b etc

*3 make a function to make queries unfold their constants in sql expressions (use GNU MP for this)