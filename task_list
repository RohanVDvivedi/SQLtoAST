PICKED TASKS


TO BE PICKED TASKS

* SQL QUERY
DDL (create alter drop)
 * create table, database and index queries
 * alter table, database and index querires
 * drop table, database and index queries
 * truncate too

* add with keywork usage
* add column aliases after table alias like
 * select * from (select * from v) as t(a,b,c)

* flatten a - b expression into a + (-b) as flat addition list, if either a or b is a constant, do that same for a/b
* make a function to make queries unfold their constants in sql expressions (use GNU MP for this)
