PICKED TASKS

DDL query
 * create queries
	SQL_TABLE,
	SQL_VIEW,
	SQL_INDEX,
	SQL_FUNCTION,
	SQL_PROCEDURE,
	SQL_TYPE,
	SQL_DOMAIN,
	SQL_SEQUENCE,
	SQL_TRIGGER,
 * alter queries
 	SQL_SCHEMA,
	SQL_TABLE,
	SQL_VIEW,
	SQL_INDEX, // only support -> ALTER INDEX idx_name RENAME TO new_idx_name
	SQL_FUNCTION,
	SQL_PROCEDURE,
	SQL_TYPE,
	SQL_DOMAIN,
	SQL_SEQUENCE,
	SQL_TRIGGER,

TO BE PICKED TASKS

* add WITH keywork usage

* make a function to make queries unfold their constants in sql expressions (use GNU MP for this)

* think if we still want this?
* prepared statements and the dynamic sql
 * named paramaters can be a problem, if we also do constant unfolding and we might loose the order of the params and may end up with wrong params at wrong places
 * we need to support just ?, ?1, ?2, etc and also :a, :b etc