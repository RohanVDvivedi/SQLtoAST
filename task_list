PICKED TASKS

* LOGICAL QUERY PLAN (read about relational algebra)
  * start with making structs fro these
   * join left/right/inner/outer
   * non_join semi and anti joins
   * aggregation with/without group_by_expressions, with/without aggregate_expressions
   * projection (add drop column attribute based projection if there is no need of it in later stages of the pipeline) (computed column can be computed at last most time)
   * selection (and can be broken into several stages alloing operator push down)
   * relation_scan (on range selection push down can become index scan)
   * union
   * sort (may be eliminated if the input row is already sorted from previous sorting or in index scan)
   * limit/offset operator

* write new expression evaluator that uses a byte code array and a slot machine to evaluate expressions, but uses the same expression evaluator

TO BE PICKED TASKS

 remaining parts of parsing DDL query
 * create/alter SQL_TYPE
   * deprioritized because RhenDB will not have user defined types and everything is off-loaded to jsonb
 * create SQL_FUNCTION/SQL_PROCEDURE
   * deprioritized because RhenDB will at the present moment rely solely on built-in functions and will not support procedures for now
 * pending create queries
	SQL_DOMAIN,
	SQL_SEQUENCE,
	SQL_TRIGGER,
 * pending alter queries
	SQL_DOMAIN,
	SQL_SEQUENCE,
	SQL_TRIGGER
   * deprioritzed because they are arcane