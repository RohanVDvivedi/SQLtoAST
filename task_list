
PICKED TASKS
 * SELECT query remaining clauses
 * subquery in the from and join clauses

* SQL QUERY
DQL (select) and then DML (insert update delete)
 * start with select query
 * where, order by, from, join, group by, having clauses, decalre them in the order they appear in the generic unoptimized execution plan
 * any of these clause can point to a expression or a sub query
 * in operator can also point to a sub query
 * same pieces can also be used in insert, update and delete queries
DDL (create alter drop)
 * start with create table, database and index queries
 * start with alter table, database and index querires
 * start with drop table, database and index queries

*SQL EXPRESSIONS that work with sub queries
 * ANY, SOME, ALL :::::           a < ANY (select score from students)
 * IN / NOT INT ::::::            a in (select id from students where passed = true)
 * EXISTS / NOT EXISTS :::::::
 * UNIQUE ::::
 etc (check for all operators like these)

--------

 * read about SQL ANSI standard
 * keep AST and logical query plan separate
 * we only build logical query plan

 * add lexer and parser rules
 * make it build AST