* SQL EXPRESSION
 * add '' token for strings in lexer
 * add || operator for concatenation of strings
 * add [:] operator for subscringing the strings
 * add logic for flattening and, or, xor, + (with -b for negatives) and * operators
 * add logic for in operator


 * read about SQL ANSI standard
 * keep AST and logical query plan separate
 * we only build logical query plan

 * add lexer and parser rules
 * make it build AST